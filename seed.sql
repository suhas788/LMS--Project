-- Create database and tables will be managed by Sequelize sync
-- Use the API to register demo users or insert directly (passwords must be bcrypt hashes)

CREATE DATABASE IF NOT EXISTS genzed_db;
USE genzed_db;
